# Production strict security profile (API)
# Use this profile after AV availability has been stabilized.

# Proxy trust configuration
# Current repo topology recommendation:
# Internet -> nginx -> api
TRUSTED_PROXY_COUNT=0

# CV upload AV scanning (strict mode)
AV_SCAN_ENABLED=true
AV_SCAN_FAIL_CLOSED=true
CLAMAV_HOST=clamav
CLAMAV_PORT=3310
CLAMAV_TIMEOUT_MS=2500

