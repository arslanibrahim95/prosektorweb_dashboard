/**
 * Shared constants for inbox API routes
 */

/**
 * Cache TTL (Time To Live) in seconds for inbox count queries
 * This prevents excessive database queries for count operations
 */
export const INBOX_COUNT_CACHE_TTL_SEC = 30;
