/**
 * Admin Panel React Query Hooks
 *
 * REFACTORED: This file is now a backward-compatible re-export hub.
 * All hook implementations are split into feature-based modules under ./admin/.
 *
 * Import path: '@/hooks/use-admin' still works.
 * New imports can use: '@/hooks/admin' directly.
 */

export * from './admin';
